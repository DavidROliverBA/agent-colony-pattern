# macOS
.DS_Store
._*

# Editors
.idea/
.vscode/
*.swp
*~

# Obsidian (if anyone clones into a vault)
.obsidian/

# Node (build tooling for diagram export only)
node_modules/
package-lock.json

# The failed excalidraw-to-svg experiment — keep the Python exporter, not the JS
scripts/export-svg.js
